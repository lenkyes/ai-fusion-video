"use client";
import { useState } from "react";
import ImageInput from "@/components/dashboard/image-input";
import { Plus, X } from "lucide-react";
export default function ImageGenPage(){const [refs,setRefs]=useState<string[]>([""]);return <div className="p-6"><h1 className="text-xl font-semibold">AI生图</h1><div className="mt-6 max-w-xl rounded-xl border p-4"><div className="mb-3 flex items-center justify-between"><span className="text-sm font-medium">参考图（可选，支持多张）</span><button type="button" className="inline-flex items-center text-xs text-primary" onClick={()=>setRefs([...refs,""])}><Plus className="mr-1 h-3 w-3"/>添加参考图</button></div>{refs.map((url,i)=><div className="mb-3 flex items-start gap-2" key={i}><div className="min-w-0 flex-1"><ImageInput value={url} onChange={v=>setRefs(xs=>xs.map((x,j)=>j===i?v:x))} uploadSubDir="image-generation"/></div>{refs.length>1&&<button type="button" className="mt-2 text-destructive" onClick={()=>setRefs(xs=>xs.filter((_,j)=>j!==i))}><X className="h-4 w-4"/></button>}</div>)}<p className="text-xs text-muted-foreground">每一项都可以切换“上传”或“链接”模式，生成时会提交全部参考图。</p></div></div>}
